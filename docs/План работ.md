## 0 Основа репозитория

✅ Vite + React + TS

✅ pnpm через Corepack + guard only-allow pnpm

✅ базовые скрипты и README (в процессе дополняем)

## 1 Code style и контроль качества

✅ ESLint (TanStack config + доп плагины)

✅ Prettier (включая Tailwind plugin)

✅ lint-staged + husky (pre-commit)

✅ commitlint + conventional commits (кастомные правила/сообщения)

✅ .editorconfig

✅ .gitattributes (LF/нормализация)

✅ запрет “чужих” lockfiles (скрипт проверки)

✅ GitHub Actions CI (lint/format/typecheck/test/commitlint на PR)

## 2 Архитектура проекта

✅ FSD каркас папок в src

✅ алиасы (@ в Vite + tsconfig paths)

✅ перенос стартовых файлов по слоям (bootstrap/init/providers)

## 3 Data-инфраструктура

✅ Registry эндпоинтов (method/path + request/response types + error shape)

✅ единый httpClient (axios instance)

✅ interceptors: единая точка обработки и нормализация ошибок

✅ request helper (типизированный вызов endpoint)

✅ TanStack Query обвязка (queryKey/queryFn helpers + Provider)

## 4 Моки и тесты

✅ MSW для dev (start до render)

✅ MSW для Vitest (Node server)

✅ сценарии (happy/401/403/500/timeout/network) + overrides

✅ stateful mocks (пример /me, PATCH → 204 + invalidate)

✅ базовые интеграционные тесты (happy, error normalization, mutation+invalidate)

✅ “по-взрослому” примеры для :params и query params (handlers + factories)

✅ генерация реалистичных данных (id/даты/списки) + reset/seed (если нужно)

✅ docs: отдельный docs/msw.md

⏳ docs/msw.md - примеры

## 5 UI foundation

✅ Design tokens (CSS variables)

✅ Тёмная тема (light/dark/system) + подключение к Tailwind

✅ ErrorScreen (единый UI “что-то пошло не так”)

✅ RouteErrorBoundary + NotFoundScreen (TanStack Router)

✅ Глобальный AppErrorBoundary

✅ InlineError (локальные ошибки для виджетов/мутаций) — отложили

✅ PageQueryGuard (страничный guard для pending/error/success) — отложили

✅ базовые UI-компоненты на токенах (Button/Card/Input) или минимальный набор

## 6 Документация шаблона

⏳ docs: как добавлять endpoint (registry + handler + сценарии)

⏳ docs: правила коммитов, pnpm, CI

⏳ docs: структура FSD и алиасы

⏳ “Quick start” раздел в корневом README

## 7 (Опционально, позже)

⏳ Storybook (витрина компонентов)

⏳ i18n, feature flags, env schema, telemetry/web-vitals и т.п. (по желанию)
